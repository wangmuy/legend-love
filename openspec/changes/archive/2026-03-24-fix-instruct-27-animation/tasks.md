## 1. 修复初始状态贴图被重置问题

- [ ] 1.1 分析 `startNewGame` 后 `JY.MyPic` 被重置的原因（`GetMyPic()` 或 `Init_SMap`）
- [ ] 1.2 修改 `GAME_SMAP.update`，在非动画期间不重置 `JY.MyPic`
- [ ] 1.3 确保 `startNewGame` 设置的初始贴图（躺着）保持到动画开始
- [ ] 1.4 测试初始状态主角正确显示为躺着

## 2. 修复动画和对话时序问题

- [ ] 2.1 分析当前动画在对话期间播放的原因
- [ ] 2.2 修改 `instruct_27`，使用 `scheduler:yield("animation")` 正确等待动画完成
- [ ] 2.3 确保动画完成后才继续执行后续对话
- [ ] 2.4 测试动画和对话的正确时序：对话A -> 动画 -> 对话B

## 3. 完善事件驱动动画系统

- [x] 3.1 添加全局动画状态 `JY.AnimationState`
- [x] 3.2 修改 `GAME_SMAP.update`，添加动画驱动逻辑
- [x] 3.3 修改 `CoroutineScheduler:update`，支持 `waitingFor == "animation"`
- [ ] 3.4 测试动画状态驱动机制

## 4. 回归测试

- [ ] 4.1 测试软体娃娃动画（id=1）正常
- [ ] 4.2 测试其他场景动画正常
- [ ] 4.3 测试非动画期间主角显示正常
- [ ] 4.4 测试动画帧率符合原版（22帧，约3.3秒）
