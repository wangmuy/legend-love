{
  "name": "jylegend-web-mud",
  "version": "0.1.0",
  "private": true,
  "description": "金庸群侠传 Web MUD — 纯文字终端版",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "node scripts/build.js",
    "start": "cd dist && python3 -m http.server 8080",
    "deploy": "npx gh-pages -d dist"
  },
  "dependencies": {
    "fengari-web": "^0.1.4",
    "xterm": "^5.3.0",
    "@xterm/addon-fit": "^0.11.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}