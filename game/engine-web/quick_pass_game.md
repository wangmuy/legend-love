# 金庸群侠传 Web MUD - 完整通关操作手册

本文档记录从开局到通关的完整用户操作流程，对应 `walkthrough-p1~p10` 自动化测试。
每个步骤均为纯用户命令（`leave`/`list`/`look`/`choose <N>`），可在终端中手动验证。

---

## 操作说明

- `choose N` — 选择菜单项/交互对象/对话选项
- `list` — 在大地图列出可去场景（按名称排序，顺序稳定）
- `leave` — 离开当前场景返回大地图
- `look` — 查看当前位置或战场态势
- `menu` — 打开主选单（医疗/解毒/状态/物品/队伍/存档/踢人）
- `help` — 显示帮助

### gotoScene 自动导航流程

```
leave           ← 回到大地图
list            ← 列出可去场景（按名称排序）
choose <idx>    ← 按名称找到场景索引，导航
```

### 验证清单（手动操作时可对照）

- 南贤居 → 显示"一间普通的民居"（非客栈）
- 田伯光居 → 只搜索鸳刀，不招田伯光（避免道德-6，保证狄云可加入）
- 闫基战斗 → 战斗胜利后显示获奖信息
- 各场景描述应与场景类型匹配（民居/山洞/客栈等）

### 存档操作说明

通过 `menu` 主选单进行存档/读档，使用纯用户命令：
- `menu` → `choose 6` → `choose N`（存到槽位 N，N=1~10）
- `menu` → `choose 6` → `choose N+10`（读取槽位 N，如 choose 11=读取槽位1）
- 存档时先 `menu` 打开主选单，选"6. 系统（存档/读档）"，再选槽位
- 读档时同样进入系统菜单，选对应读取选项

| 阶段 | 槽位范围 |
|------|---------|
| P1 | 1~6 |
| P2 | 1~2 |
| P3 | 1~2 |
| P4 | 1~2 |
| P5 | 1~3 |
| P6 | 1~3 |
| P7 | 1~2 |
| P8 | 1 |
| P9 | 1 |
| P10 | 1 |

---

## P1: 开局→南贤→田伯光居→闫基→铁掌→段誉→无量山洞

### Step 1: 开局（存档槽位 1）
```
> choose 1           ← 选"重新开始"
（显示人物属性：生命、攻击等）
> choose 1           ← 确认属性
（游戏开始，显示软体娃娃对话，出现在 主角的家）
> leave              ← 离开回家到大世界（到大地图MMAP）
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 2: 南贤居对话（scene 64，读取槽位1 → 存到槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"南賢居"的索引...
> choose <idx>       ← 导航到南賢居
（到达南賢居，显示"一间普通的民居"）
> choose 2           ← 选择 Entity 2=南贤（Entity 1=柜子/搜索）
> choose 1           ← 选择"对话"
（南贤对话剧情，得罗盘→柜子中）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 3: 田伯光居（scene 59）— 搜索鸳刀，不招田伯光（避免道德-6，读取槽位1 → 存到槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"田伯光居"的索引...
> choose <idx>       ← 导航到田伯光居
（到达田伯光居，显示"一间普通的民居"）
> choose 1           ← 搜索 Entity 1（得**鸳刀** item 187 + 黑血神针×5）
（鸳刀用于鸳鸯岛 P8 Step 2，与鸯刀配对开锁得《鸳鸯刀》天书）
> leave              ← 返回大地图
```
> **注意**: 不要与田伯光对话招人，否则道德-6，导致后续狄云无法加入（需道德60+）。
**存档**: `menu` → `choose 6` → `choose 2`（存到槽位2）

### Step 4: 唐诗山东/山洞（scene 65）— 唐诗选辑（连城诀前置，读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...在场景列表中找到"山洞"（scene 65，坐标364:279附近，有4个同名"山洞"场景，注意区分）...
> choose <idx>       ← 导航到唐诗山东（scene 65）
（显示"你来到了"，Entity 1=oldevent_838, Entity 2=oldevent_602）
> choose 1           ← 搜索 Entity 1（oldevent_838，得硝石×30）
> choose 2           ← 搜索 Entity 2（oldevent_602，得**唐诗选辑** item 160，连城诀前置）
> leave              ← 返回大地图
```

### Step 5: 闫基居（scene 50）— 战斗得胡家刀法/天王保命丹（读取槽位2 → 存到槽位3）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"閰基居"的索引...
> choose <idx>       ← 导航到闫基居
（到达闫基居）
> choose 4           ← 选择瓦片事件 #4（阎基战斗）
（剧情对话，显示"阎基"）
> look               ← 查看战场态势
> choose 5           ← 选择"移动"
> choose 1           ← 选择"走近"
> choose 1           ← 选择"攻击"
> choose 1           ← 选择目标（阎基）
（循环：choose 1 → choose 1 直到"战斗胜利"或"战斗失败"）
> look               ← 查看战斗结果
> choose 1           ← 搜索 Entity 1（得两页刀法，触发胡斐加入剧情的关键道具）
> choose 1           ← 搜索 Entity 1（得药材）
> choose 1           ← 搜索 Entity 1（得天王保命丹）
> leave              ← 返回大地图
```
（战后得 两页刀法 + 胡家刀法 + 天王保命丹）
**存档**: `menu` → `choose 6` → `choose 3`（存到槽位3）

### Step 6: 铁掌山（scene 48）— 只取大燕皇帝世系图表，跳过铁掌拳谱（避免道德-1，读取槽位3 → 存到槽位4）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 13         ← 读取槽位3
> list
...找到"鐵掌山"的索引...
> choose <idx>       ← 导航到铁掌山
（到达铁掌山）
> choose 3           ← Entity 3=oldevent_453（得银两3000+**大燕皇帝世系图表**）
> leave              ← 返回大地图
```
> **注意**: 不要搜索 Entity 4（oldevent_454），否则道德-1，导致后续狄云无法加入（需道德60+）。
（大燕皇帝世系图表→燕子坞给慕容复触发剧情）
**存档**: `menu` → `choose 6` → `choose 4`（存到槽位4）

### Step 7: 高升客栈（scene 61）→ 段誉加入（读取槽位4 → 存到槽位5）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 14         ← 读取槽位4
> list
...找到"高昇客棧"的索引...
> choose <idx>       ← 导航到高升客栈
（到达高升客栈）
> choose 5           ← 选择 Entity 5=段誉
> choose 1           ← 选择"对话"
（对话剧情，出现"是否要求加入？"）
> choose 1           ← 选"是"（段誉加入，显示"段誉"）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 5`（存到槽位5）

### Step 8: 无量山洞（scene 42）— 段誉教凌波微步（读取槽位5 → 存到槽位6）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 15         ← 读取槽位5
> list
...找到"無量山洞"的索引...
> choose <idx>       ← 导航到无量山洞
（到达无量山洞，段誉在队伍中）
> choose 1           ← 选择 Entity 1=段誉
> choose 1           ← 选择"对话"
（段誉教凌波微步，显示"段誉"）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 6`（存到槽位6）

---

## P2: 回族部落→胡斐加入→苗人凤退敌→冰火岛→绝情谷→冰蚕洞→大轮寺

### Step 1: 回族部落（scene 17）— 霍青桐对话接可兰经任务（读取槽位6）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 16         ← 读取槽位6
> list
...找到"回族部落"的索引...
> choose <idx>       ← 导航到回族部落
（显示"你来到了"，Entity 1=守卫, Entity 2=霍青桐）
> choose 2           ← 选择霍青桐（Entity 2，oldevent_620）
> choose 1           ← 选择"对话"
（霍青桐讲述可兰经被金轮法王夺走，接可兰经任务，触发金轮寺战斗入口出现）
> leave              ← 返回大地图
```
（NPC 对话设置金轮寺 D* 事件，可兰经→回族部落得《书剑恩仇录》）

### Step 2: 胡斐加入（胡斐居 scene 0 — 使用两页刀法，存到槽位1）
```
> list
...找到"胡斐居"的索引...
> choose <idx>       ← 导航到胡斐居
（显示"你来到了"）
> menu              ← 打开主选单
> choose 4          ← 选"物品"
> choose 1          ← 选"使用"
> choose <itemIdx>  ← 选"兩頁刀法"（从闫基居搜索获得）
> choose <npcIdx>   ← 选"胡斐 (场景NPC)"
（两页刀法剧情对话，出现"是否要求加入？"）
> choose 1           ← 选"是"（胡斐加入队伍）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 3: 苗人凤居（scene 24）退敌 — 获得道德+2（读取槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"苗人鳳居"的索引...
> choose <idx>       ← 导航到苗人凤居
（显示"你来到了"）
> look               ← 查看实体列表
> choose N           ← 选择"搜索"实体（eventExtra=30，退敌战斗）
（触发苗人凤退敌战斗[3]，战斗胜利后获得道德+2）
> leave              ← 返回大地图
```
> **道德增益**: 胡斐加入后，苗人凤居会出现退敌事件，战斗胜利+2道德。

### Step 4: 冰火岛（scene 72）/金毛（读取槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"冰火島"的索引...
> choose <idx>       ← 导航到冰火岛
（显示"你来到了"）
> choose 1           ← Entity 1=谢逊对话
> choose 2           ← Entity 2=得一撮金毛（item 181）
> leave              ← 返回大地图
```

### Step 5: 绝情谷（scene 22）/玉玺/断肠草/君子剑/玉蜂针（存到槽位2）
```
> list
...找到"絕情谷"的索引...
> choose <idx>       ← 导航到绝情谷
（显示"你来到了"，5个实体均为搜索/触发事件）
> look               ← 查看实体列表
> choose N           ← 选君子剑+淑女剑（oldevent_400，item 107+108）
> choose N           ← 选玉玺（oldevent_401，item 130）
> choose N           ← 选药材×100+智慧果（oldevent_856，item 171+186）
> choose N           ← 选玉蜂针×10+无常丹×6（oldevent_857）
> choose N           ← 选断肠草（oldevent_399，item 134）
> leave              ← 返回大地图
```
（断肠草→神雕洞救杨过，玉玺→燕子坞给慕容复）
> **注意**：场景列表同时含"絕情谷"与"絕情谷底"，`choose` 时必须选择"絕情谷"那一项
> （排序靠前），选成"絕情谷底"会进入杨过重逢剧情导致本步无法搜刮。
**存档**: `menu` → `choose 6` → `choose 2`（存到槽位2）

### Step 6: 冰蚕洞（scene 66）/天山雪莲（读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"冰蠶洞"的索引...
> choose <idx>       ← 导航到冰蚕洞
（显示"你来到了"，Entity 1=天山雪莲+钥匙，Entity 2=千年冰蚕）
> choose 1           ← Entity 1（得天山雪莲、银钥匙、黑血神针、智慧果）
> choose 1           ← Entity 2（得千年冰蚕）
> leave              ← 返回大地图
```

### Step 7: 大轮寺（scene 8）— 守门僧兵战→钥匙孔→狄云对话加入+搜刮（船停大轮寺东）
```
> list
...找到"大輪寺"的索引...
> choose <idx>       ← 导航到大轮寺
（显示"你来到了"，Entity 1=守门僧兵, Entity 2=入口, Entity 3=狄云, Entity 4=宝箱, Entity 5=宝箱, 另有搜索事件）
> choose 1           ← 选择守门僧兵（Entity 1）
> look               ← 查看战场态势
> choose 5           ← 选择"移动"
> choose 1           ← 选择"走近"
> choose 1           ← 选择"攻击"
> choose 1           ← 选择目标
（循环战斗直到"战斗胜利"，得+1声望，守门僧兵事件消失）
> look               ← 查看战斗结果
> choose N           ← 选择"搜索"实体（eventExtra=596，第二场守门僧兵战斗）
> look               ← 查看战场态势
> choose 5           ← 选择"移动"
> choose 1           ← 选择"走近"
> choose 1           ← 选择"攻击"
> choose 1           ← 选择目标
（循环战斗直到"战斗胜利"，得**橙钥匙** item 162 + 声望）
（橙钥匙用于大轮寺钥匙孔，得道德+2）
> choose N           ← 选择"搜索"实体（eventTouch=598，钥匙孔）
（使用橙钥匙打开钥匙孔，道德+2）
> choose 3           ← 选择狄云（Entity 3，oldevent_599，对话触发连城诀剧情）
> choose 1           ← 选择"对话"
（长对话，狄云讲述经历并给出唐诗山洞坐标"364279"）
> choose 1           ← 选"是"（狄云加入队伍，需道德60+）
（若道德≥60：狄云加入，道德+3，后续可触发连城诀剧情）
（若道德<60：狄云拒绝加入，"不了！我这个不幸之人还是别害人的好"，但坐标已给出，不影响连城诀）
> choose 4           ← 搜索 Entity 4（oldevent_839，得六阳正气丹×2+智慧果×2+飞蝗石×5）
> choose 5           ← 搜索 Entity 5（oldevent_853，得银两400）
> leave              ← 返回大地图
```
（狄云提示唐诗山洞坐标364279，即唐诗山东/山洞场景；狄云→连城诀剧情线）
> **道德要求**: 狄云加入需要道德≥60。按本攻略最优路径（不招田伯光、跳过铁掌拳谱、胡斐加入+3、苗人凤退敌+2、大轮寺钥匙孔+2），道德可达57，仍需3点。若道德不足，可后续通过其他剧情提升后再回来招狄云，或跳过狄云加入继续连城诀（坐标已给出）。

---

## P3: 百花谷→绝情谷底→古墓→燕子坞→泰山派（含昆仑仙境/神雕洞）

### Step 1: 昆仑仙境/张无忌加入/九阳真经（读取槽位2，需有金毛）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"崑崙仙境"的索引（scene 67，入口坐标22:439 的洞口场景）...
> choose <idx>       ← 导航到昆仑仙境（洞口）
（显示"你来到了"）
> look               ← 查看出口列表
> choose <exitIdx>   ← 选出口（→崑侖仙境 scene 4，张无忌所在；该场景无地图入口只能经出口进入）
（进入 scene 4 崑侖仙境，张无忌出现）
> look               ← 查看实体（张无忌为动态 D* 实体，编号不固定，按文本定位）
> choose <zjIdx>     ← 选张无忌（oldevent_70 对话，多页对话连续 choose 1 直到"交谈结束"）
> choose 0           ← 关闭对话框
> leave              ← 返回大地图
> list
...重新导航到崑崙仙境（scene 67）→ 选出口 → 进入 scene 4 ...
（第二次访问：张无忌事件变为 75（普通对话）/ 71（使用金毛））
> menu              ← 打开主选单
> choose 4          ← 选"物品"
> choose 1          ← 选"使用"
> choose <itemIdx>  ← 选"一撮金毛"（item 181）
> choose <npcIdx>   ← 选"张无忌"（场景NPC）
（使用金毛触发 oldevent_71，消耗金毛，张无忌加入队伍）
> leave              ← 返回大地图
```

### Step 2: 神雕洞/救杨过（需有断肠草）
```
> list
...找到"神鵰洞"的索引...
> choose <idx>       ← 导航到神雕洞
（显示"你来到了"，Entity 1=杨过）
> choose 1           ← 选择杨过（触发对话）
（杨过剧情对话，出现"是否使用物品[断肠草]？"）
> choose 1           ← 选"是"（消耗断肠草，救活杨过，得玉蜂浆）
（出现"是否要求加入？"）
> choose 1           ← 选"是"（杨过加入队伍）
> leave              ← 返回大地图
```

### Step 3: 百花谷/养蜂
```
> list
...找到"百花谷"的索引...
> choose <idx>       ← 导航到百花谷
（显示"你来到了"，Entity 1=周伯通）
> choose 1           ← 选择周伯通（对话，可触发养蜂剧情）
> leave              ← 返回大地图
```

### Step 4: 绝情谷底 — 杨过小龙女重逢（存到槽位1）
```
> list
...找到"絕情谷底"的索引...
> choose <idx>       ← 导航到绝情谷底
（显示"你来到了"，Entity 1=杨过(oldevent_436)）
> choose 1           ← 选择杨过（Entity 1，触发剧情）
> choose 1           ← 连续对话（杨过和小龙女在绝情谷底重逢的完整剧情）
（杨过+小龙女重逢剧情→杨过学会黯然销魂掌→离开队伍返回古墓）
> choose 0           ← 关闭对话框
> leave              ← 返回大地图
```
（关键剧情：杨过在队伍中触发「绝情谷底重逢」，把小龙女+杨过+九阴真经+神雕侠侣事件放入古墓 D* 表。若杨过不在队伍则仅对话，小龙女仍会去古墓。之后去古墓可找到他们。）
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 5: 古墓/小龙女加入/九阴真经/《神雕侠侣》（读取槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"古墓"的索引...
> choose <idx>       ← 导航到古墓
（显示"你来到了"）
> choose 1           ← 选择 NPC 小龙女
> choose 1           ← 选择"对话"
> choose 1           ← 选"是"（小龙女加入）
> choose 2           ← 搜索 Entity 2（得九阴真经 item 94，oldevent_442）
> choose 1           ← 对话抄录九阴真经
> choose 3           ← 搜索 Entity 3（得《神雕侠侣》天书 item 153，oldevent_443）
> leave              ← 返回大地图
```

### Step 6: 燕子坞/慕容复、王语嫣加入（存到槽位2）
```
> list
...找到"燕子塢"的索引...
> choose <idx>       ← 导航到燕子坞
（显示"你来到了"）
> choose 1           ← 选择 NPC 慕容复
> choose 1           ← 选择"对话"
> choose 1           ← 选择"是"（慕容复加入）
> choose 2           ← 选择 NPC 王语嫣（Entity 2）
> choose 1           ← 选择"对话"
> choose 1           ← 选"是"（王语嫣加入）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 2`（存到槽位2）

### Step 7: 泰山派/洗手帖（读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"泰山派"的索引...
> choose <idx>       ← 导航到泰山派
（显示"你来到了"，共6个实体：前2为NPC对话/战斗，后4为宝箱搜索）
> look               ← 查看实体列表
> choose 1           ← Entity 1=天门（oldevent_176，对话）
> choose 1           ← Entity 2=oldevent_174（对话，触发战斗[25]）
> choose 1           ← Entity 3=宝箱（oldevent_879，银两150+智慧果2）
> choose 1           ← Entity 4=宝箱（oldevent_880，得**金盆洗手请帖** item 193）
> choose 1           ← Entity 5=宝箱（oldevent_881，药材10+智慧果2）
> choose 1           ← Entity 6=宝箱（oldevent_882，玉真散2）
> leave              ← 返回大地图
```

---

## P4: 苗人凤→蝴蝶谷→程瑛→黑龙潭→一灯居→闫基居

### Step 1: 苗人凤居/眼毒后续（退敌战斗已在 P2 Step3 完成，此步为后续剧情准备，读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"苗人鳳居"的索引...
> choose <idx>       ← 导航到苗人凤居
（显示"你来到了"，退敌战斗已在 P2 Step3 完成，眼毒剧情待后续触发）
> leave              ← 返回大地图
```
> **注意**: 退敌战斗（道德+2）已在 P2 Step3 提前完成。此步只需路过确认场景状态，后续在 P5 获得苗人凤眼药后再来治疗眼毒（道德+3）。

### Step 2: 蝴蝶谷/铲子/胡青牛加入
```
> list
...找到"蝴蝶谷"的索引...
> choose <idx>       ← 导航到蝴蝶谷
（显示"你来到了"）
> look               ← 查看场景实体
> choose 5           ← 胡青牛（第5NPC）
> choose 1           ← 对话
（多页对话，连续 choose 1 跳过，出现"是否要求加入？"）
> choose 1           ← 是（胡青牛加入）
> leave              ← 返回大地图
```
（注：恒山派→蜘蛛洞→悦来客栈场景已可用）

### Step 3: 恒山派
```
> list
...找到"恒山派"的索引...
> choose <idx>       ← 导航到恒山派
（显示"你来到了"，Entity 1=守门弟子）
> choose 1           ← 选择守门弟子（对话，触发恒山派相关剧情）
> leave              ← 返回大地图
```

### Step 4: 蜘蛛洞/玄冰碧火酒
```
> list
...找到"蜘蛛洞"的索引...
> choose <idx>       ← 导航到蜘蛛洞
（显示"你来到了"）
> look               ← 查看场景实体
> choose 6           ← 选择 Entity 6（oldevent_372，得**玄冰碧火酒** item 136）
（玄冰碧火酒→摩天崖给石破天喝，触发加入，为《侠客行》做准备）
> leave              ← 返回大地图
```

### Step 5: 悦来客栈/令狐冲喝酒
```
> list
...找到"悅來客棧"的索引...
> choose <idx>       ← 导航到悦来客栈
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 3           ← 选择令狐冲（第3NPC）
> choose 1           ← 对话
（令狐冲喝酒剧情，多页对话连续 choose 1 跳过）
> leave              ← 返回大地图
```

### Step 6: 摩天崖/石破天加入+白龙剑（需先拿玄冰碧火酒，为《侠客行》做准备）
```
> list
...找到"摩天崖"的索引...
> choose <idx>       ← 导航到摩天崖（scene 38）
（显示"你来到了"，Entity 1=oldevent_336, Entity 2=石破天(oldevent_333)）
> choose 1           ← 搜索 Entity 1（oldevent_336，得白龙剑 item 112）
> choose 2           ← 选择石破天（对话剧情，走火入魔，oldevent_333→334→335）
> menu              ← 打开主选单
> choose 4          ← 选"物品"
> choose 1          ← 选"使用"
> choose <itemIdx>  ← 选"玄冰碧火酒"（从蜘蛛洞获得，item 136）
> choose <npcIdx>   ← 选"石破天 (场景NPC)"
（oldevent_335 触发：石破天喝下玄冰碧火酒，走火入魔痊愈，出现"是否要求加入？"）
> choose 1           ← 选"是"（石破天加入队伍，用于侠客岛得《侠客行》）
> leave              ← 返回大地图
```

### Step 7: 五毒教/苗人战斗（需准备战斗）
```
> list
...找到"五毒教"的索引...
> choose <idx>       ← 导航到五毒教（scene 37）
（显示"你来到了"，Entity 1=苗人守卫(触发战斗[96])）
> choose 1           ← 选择守卫（触发对话→战斗[96]）
> look               ← 查看战场态势
（战斗循环直到"战斗胜利"，得+1声望）
> leave              ← 返回大地图
```

### Step 8: 程瑛居/程瑛加入（存到槽位1）
```
> list
...找到"程瑛居"的索引...
> choose <idx>       ← 导航到程瑛居
（显示"你来到了"）
> look               ← 查看场景实体
> choose 2           ← 程英（第2NPC）
> choose 1           ← 对话
（多页对话，连续 choose 1 跳过，出现"是否要求加入？"）
> choose 1           ← 是（程英加入）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 9: 黑龙潭（读取槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"黑龍潭"的索引...
> choose <idx>       ← 导航到黑龙潭
（显示"你来到了"）
> look               ← 查看场景实体
> choose 1           ← 选择 tile event 416（程英破阵，需程英在队伍中）
> choose 2           ← 选择瑛姑（第2NPC，oldevent_417）
> choose 1           ← 选择"对话"
（瑛姑对话剧情，多页对话连续 choose 1 跳过）
> leave              ← 返回大地图
```
（注：黑龙潭二刷已验证，程英在队伍中可触发破阵事件，去一灯居后返回黑龙潭二刷可获得桃花岛位置）

### Step 10: 一灯居（需先完成瑛姑对话）
```
> list
...找到"一燈居"的索引...
> choose <idx>       ← 导航到一灯居
（显示"你来到了"，Entity 1=一灯大师）
> choose 1           ← 选择一灯（对话，需瑛姑手帕才触发深处剧情）
> leave              ← 返回大地图
```

### Step 11: 闫基居/七星海棠（需胡斐在队伍中，存到槽位2）
```
> list
...找到"閰基居"的索引...
> choose <idx>       ← 导航到闫基居
（显示"你来到了"）
> choose 2           ← 选择阎基（当胡斐在队中触发旧事，胡家刀法缺页）
（阎基认罪剧情，出现"是否与之过招？"对话框）
> choose 1           ← 选"是"（进入战斗，战斗胜利得七心海棠）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 2`（存到槽位2）

---

## P5: 药王庄→金轮寺→明教分舵→光明顶→华山→金蛇洞→武当→嵩山

### Step 1: 药王庄/眼药/程灵素加入（读取槽位2 → 存到槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"藥王莊"的索引...
> choose <idx>       ← 导航到药王庄
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 1           ← 选择程灵素（第1NPC）
> choose 1           ← 选择"对话"
> choose 1           ← 选"是"（程灵素加入）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 2: 金轮寺/可兰经（读取槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"金輪寺"的索引...
> choose <idx>       ← 导航到金轮寺
（显示"你来到了"，Entity 1=守门僧兵(战斗), Entity 2=金轮法王, etc.）
> choose 1           ← 选择守门僧兵（触发战斗[99]）
> look               ← 查看战场态势
（战斗循环直到"战斗胜利"）
> leave              ← 返回大地图
> list
...重新导航到金轮寺...
> choose 2           ← 选择金轮法王（触发战斗[100]）
（战斗胜利后获得可兰经 item 159）
> leave              ← 返回大地图
```
（可兰经→回族部落给霍青桐，得《书剑恩仇录》）

### Step 3: 衡山派战斗
```
> list
...找到"衡山派"的索引...
> choose <idx>       ← 导航到衡山派
（显示"你来到了"）
> choose 1           ← 选择 NPC
> look               ← 查看战场态势
（如果进入战斗：）
> choose 5           ← 移动
> choose 1           ← 走近
> choose 1           ← 攻击
> choose 1           ← 选择目标
（循环直到"战斗胜利"）
> leave              ← 返回大地图
```

### Step 4: 明教分舵/黑玉断续膏
```
> list
...找到"明教分舵"的索引...
> choose <idx>       ← 导航到明教分舵
（显示"你来到了"，Entity 1=搜索(黑玉断续膏)）
> choose 1           ← 搜索（得黑玉断续膏×5 等物品）
> leave              ← 返回大地图
```

### Step 5: 光明顶/六大派战斗/铁焰令（存到槽位2）
```
> list
...找到"光明頂"的索引...
> choose <idx>       ← 导航到光明顶
（显示"你来到了"）
> look               ← 查看场景 tile 事件
> choose 1           ← 触发六大派围攻事件（oldevent_82）
（剧情对话，显示光明顶六大派围攻剧情）
（如果进入战斗：）
> choose 5           ← 移动
> choose 1           ← 走近
> choose 1           ← 攻击
> choose 1           ← 选择目标
（循环直到"战斗胜利"）
> look               ← 重新查看场景（战斗后 NPC 出现）
> choose 1           ← 选择范遥（对话，触发 oldevent_86 剧情）
（范遥、韦一笑、殷天正对话，得明教铁焰令 item 190，用于后续冰火岛寻谢逊）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 2`（存到槽位2）

### Step 6: 华山派/对话令狐冲、岳不群（读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"華山派"的索引...
> choose <idx>       ← 导航到华山派
（显示"你来到了"，Entity 1=令狐冲, Entity 2=岳不群样等）
> choose 1           ← 选择令狐冲（对话，触发华山派剧情）
> choose 1           ← 再次对话
> leave              ← 返回大地图
```

### Step 7: 金蛇洞/金蛇剑（需武力75+）
```
> list
...找到"金蛇山洞"的索引...
> choose <idx>       ← 导航到金蛇山洞
（显示"你来到了"，Entity 1=金蛇秘笈, Entity 2=金蛇锥, Entity 3=金蛇剑）
> choose 1           ← 选 Entity 1（oldevent_641，得金蛇秘笈）
> choose 2           ← 选 Entity 2（oldevent_642，得金蛇锥×10）
> choose 3           ← 选 Entity 3（oldevent_640，拔金蛇剑，需武力≥75）
> leave              ← 返回大地图
```

### Step 8: 武当山/击败张三丰（读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"武當派"的索引...
> choose <idx>       ← 导航到武当山
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 2           ← 选择张三丰（第2NPC）
> choose 1           ← 选择"对话"
（触发战斗，显示"战场态势"）
> choose 5           ← 移动
> choose 1           ← 走近
> choose 1           ← 攻击
> choose 1           ← 选择目标
（循环直到"战斗胜利"）
> leave              ← 返回大地图
```

### Step 9: 嵩山派/张旭率意帖（存到槽位3）
```
> list
...找到"嵩山派"的索引...
> choose <idx>       ← 导航到嵩山派
（显示"你来到了"）
> choose 1
> look               ← 查看是否进入战场
（如果显示"战场态势"，说明触发了战斗：）
> choose 5           ← 移动
> choose 1           ← 走近
> choose 1           ← 攻击
> choose 1           ← 选择目标
（循环直到"战斗胜利"）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 3`（存到槽位3）

---

## P6: 神龙教→破庙→成昆→沙漠→北丑→灵蛇→渤泥→侠客

### Step 1: 神龙教《鹿鼎记》（读取槽位3）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 13         ← 读取槽位3
> list
...找到"神龍教"的索引...
> choose <idx>       ← 导航到神龙教
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 1           ← 选择洪教主（第1NPC，oldevent_609）
> choose 1           ← 选择"对话"
（洪教主对话剧情，多页对话连续 choose 1 跳过）
> leave              ← 返回大地图
```

### Step 2: 破庙/广陵散（需铁铲，存到槽位1）
```
> list
...找到"破廟"的索引...
> choose <idx>       ← 导航到破庙
（显示"你来到了"，Entity 1=阿紫, Entity 2=游坦之）
> choose 1           ← 选择阿紫（触发事件，战斗循环直到"战斗胜利"，得+2道德）
> look               ← 查看场景
> choose 3           ← 选择墓碑（触发使用铁铲事件，得广陵散琴曲 item 177）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 3: 冰火岛 — 铁焰令寻谢逊（需有明教铁焰令，读取槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"冰火島"的索引...
> choose <idx>       ← 导航到冰火岛
（显示"你来到了"，Entity 1=谢逊, Entity 2=搜索(金毛)）
> menu              ← 打开主选单
> choose 4          ← 选"物品"
> choose 1          ← 选"使用"
> choose <itemIdx>  ← 选"明教铁焰令"
> choose <npcIdx>   ← 选"谢逊 (场景NPC)"
（使用铁焰令触发 oldevent_61，谢逊认出铁焰令，对话剧情）
（此后成昆居出现成崑战斗入口）
> leave              ← 返回大地图
```

### Step 4: 成昆居/成崑战斗/一颗头颅（读取槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"成崑居"的索引...
> choose <idx>       ← 导航到成昆居
（显示"你来到了"，Entity 1~6=搜索/宝箱）
> choose N           ← 寻找并选择成崑（Entity，触发 oldevent_91 战斗[13]）
（剧情对话，成崑出现，进入战斗）
（战斗循环：choose 5→1→1→1 直到"战斗胜利"）
（胜利后得 一颗头颅 item 191，用于冰火岛换屠龙刀）
> choose 1           ← 搜索（得玄冰碧火酒 item 136）
> choose 2           ← 搜索（得黄沙万里鞭法 item 87）
> choose 3           ← 搜索（得千年灵芝 item 29）
> leave              ← 返回大地图
```

### Step 5: 沙漠废墟/高昌迷宫/《白马啸西风》
```
> list
...找到"沙漠廢墟"的索引...
> choose <idx>       ← 导航到沙漠废墟
（显示"你来到了"，Entity 1=oldevent_654，出口→高昌迷宫）
> look               ← 查看出口列表
> choose <exitIdx>   ← 选择出口→高昌迷宫（场景 14）
（到达高昌迷宫，Entity 3=oldevent_656）
> choose 3           ← 选择 Entity 3（oldevent_656，得《白马啸西风》item 149）
> leave              ← 返回大地图
```
（注：高昌迷宫实体顺序：1=oldevent_844, 2=oldevent_655(劈门，需武力90+玄铁剑), 3=oldevent_656(白马啸西风)。直接 choose 3 即可得书，无需劈门。）

### Step 6: 北丑居（存到槽位2）
```
> list
...找到"北丑居"的索引...
> choose <idx>       ← 导航到北丑居
（显示"你来到了"，Entity 1=北丑, Entity 2=箱子(需金钥匙), etc.）
> choose 1           ← 选择北丑（对话，触发剧情）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 2`（存到槽位2）

### Step 7: 灵蛇岛/紫衫龙王（读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"靈蛇島"的索引...
> choose <idx>       ← 导航到灵蛇岛
（显示"你来到了"，Entity 1=王难姑, Entity 2=獐腿肉+智慧果, Entity 3=金花婆婆）
> choose 3           ← 选择金花婆婆（entity 3，对话，触发战斗）
（战斗循环直到"战斗胜利"，得+3声望）
> choose 1           ← 选择王难姑（entity 1，对话，获救剧情）
> choose 2           ← 搜索（entity 2，得獐腿肉、智慧果）
> leave              ← 返回大地图
```

### Step 8: 渤泥岛/袁承志接任务（第一次，<碧血剑>线索，读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"浡泥島"的索引...
> choose <idx>       ← 导航到渤泥岛
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 1           ← 选择袁承志（第1NPC，oldevent_635）
> choose 1           ← 选择"对话"
（袁承志对话剧情，告知三试（智/勇/仁），需先拔金蛇剑）
> leave              ← 返回大地图
```

### Step 9: 侠客岛/龙岛主对话（第一次，<侠客行>线索，存到槽位3）
```
> list
...找到"俠客島"的索引...
> choose <idx>       ← 导航到侠客岛
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 3           ← 选择龙岛主（第3NPC，oldevent_353）
> choose 1           ← 选择"对话"
（龙岛主对话剧情，多页对话连续 choose 1 跳过，可得腊八粥）
（龙岛主提示需破解太玄经，需石破天在队伍中）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 3`（存到槽位3）

### Step 9b: 侠客岛二刷——石破天解太玄经得《侠客行》+太玄经（读取槽位3，需石破天在队）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 13         ← 读取槽位3
> list
...找到"俠客島"的索引...
> choose <idx>       ← 导航到侠客岛
（显示"你来到了"）
> look               ← 查看场景 NPC（D* 动态扫描显示 oldevent_363）
> choose <N>         ← 选择动态实体石壁（oldevent_363，需石破天在队）
（石破天领悟太玄经，龙岛主交出 <侠客行> item 154）
（石破天学会太玄神功）
> leave              ← 返回大地图
```
（需 P4 摩天崖石破天加入队伍。363 由 353 对话后 D* 表动态设置于 tile 12/13，位于静态 NPC 之后显示。）

### Step 9c: 渤泥岛二刷——袁承志交《碧血剑》（读取槽位3，需金蛇剑+道德80+）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 13         ← 读取槽位3
> list
...找到"浡泥島"的索引...
> choose <idx>       ← 导航到渤泥岛（读取槽位3）
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 1           ← 选择袁承志（Entity 1，oldevent_636）
> choose 1           ← 选择"对话"
（袁承志检查金蛇剑+道德，道德≥80则得《碧血剑》item 156）
> leave              ← 返回大地图
```
（需 P5 金蛇洞拔金蛇剑+道德≥80）

### Step 10: 冰火岛——屠龙刀（用一颗头颅换屠龙刀，读取槽位3）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 13         ← 读取槽位3
> list
...找到"冰火島"的索引...
> choose <idx>       ← 导航到冰火岛
（显示"你来到了"，Entity 1=谢逊, Entity 2=金毛已取）
> menu              ← 打开主选单
> choose 4          ← 选"物品"
> choose 1          ← 选"使用"
> choose <itemIdx>  ← 选"一颗头颅"（from 成昆居战成崑）
> choose <npcIdx>   ← 选"谢逊 (场景NPC)"
（使用一颗头颅触发 oldevent_65，谢逊报仇雪恨）
（谢逊将屠龙刀 item 117 赠予主角，用于苗人凤居换飞狐外传）
> leave              ← 返回大地图
```
（注：屠龙刀用于 P8 苗人凤居《飞狐外传》，需+胡斐+金丝背心）

### Step 11: 回族部落二刷——交还《可兰经》得《书剑恩仇录》（读取槽位3）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 13         ← 读取槽位3
> list
...找到"回族部落"的索引...
> choose <idx>       ← 导航到回族部落
（显示"你来到了"，Entity 1=守卫, Entity 2=霍青桐）
> choose 2           ← 选择霍青桐（Entity 2，触发 oldevent_622）
> choose 1           ← 选"是"（使用可兰经）
（霍青桐收回可兰经，赠送《书剑恩仇录》item 152）
> leave              ← 返回大地图
```
（需先在 P5 Step2 金轮寺从金轮法王处获得可兰经）

### Step 12: 五毒教二刷——蓝凤凰加入/鹿鼎记线索（读取槽位3）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 13         ← 读取槽位3
> list
...找到"五毒教"的索引...
> choose <idx>       ← 导航到五毒教
（显示"你来到了"，神龙教洪教主告诉玩家韦小宝躲在五毒教）
> look               ← 查看场景实体
> choose 1           ← 选择五毒教守卫（触发战斗[97]）
（战斗循环直到"战斗胜利"）
> choose 1           ← 选择蓝凤凰（Entity，oldevent_616）
> choose 1           ← 选择"对话"
（蓝凤凰透露真相：洪教主利用玩家，韦小宝偷的是四十二章经非鹿鼎记）
> choose 1           ← 选"是"（蓝凤凰加入队伍）
> leave              ← 返回大地图
```

### Step 13: 神龙教三刷——洪教主战斗得《鹿鼎记》（读取槽位3）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 13         ← 读取槽位3
> list
...找到"神龍教"的索引...
> choose <idx>       ← 导航到神龙教
（显示"你来到了"）
> look               ← 查看场景实体（D* 动态扫描显示 oldevent_611）
> choose <N>         ← 选择动态实体洪教主（oldevent_611，战斗[95]）
（剧情：主角质问洪教主为何欺骗，进入战斗[95]）
> look               ← 查看战场态势
（战斗循环：choose 5→1→1→1 直到"战斗胜利"）
（胜利后洪教主交出《鹿鼎记》item 150）
> leave              ← 返回大地图
```
（611 由五毒教 616 线完成后设置于神龙教 tile 3，需先完成 P6 Step12 五毒教二刷。）

### Step 14: 光明顶二刷——谢逊圣火阵得《倚天屠龙记》（读取槽位3，需已冰火岛见过谢逊）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 13         ← 读取槽位3
> list
...找到"光明頂"的索引...
> choose <idx>       ← 导航到光明顶
（显示"你来到了"）
> look               ← 查看场景实体（D* 动态扫描显示 oldevent_115）
> choose <N>         ← 选择动态实体谢逊（oldevent_115，圣火阵）
> choose 5           ← 选"战斗"（挑战光明圣火阵）
（与谢逊+范遥+杨逍+殷天正+韦一笑+金花婆婆战斗[15]）
> look               ← 查看战场态势
（战斗循环：choose 5→1→1→1 直到"战斗胜利"）
（胜利后谢逊赠送《倚天屠龙记》item 155）
> leave              ← 返回大地图
```
（需先 P5 Step5 得铁焰令 → P6 Step3 冰火岛使用铁焰令见谢逊 → P6 Step7 灵蛇岛金花婆婆对话(oldevent_105)后，光明顶 111~116 圣火阵实体才出现；其中 tile 94 = oldevent_115 圣火阵。）

---

## P7: 福威镖局→天宁寺→梅庄→黑木崖→丐帮→桃花岛→主角居→黑木崖内部→丐帮→天宁寺

### Step 1: 福威镖局/溪山行旅图（读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"福威鏢局"的索引...
> choose <idx>       ← 导航到福威镖局
（显示"你来到了"，Entity 1=林平之, Entity 2=宝箱(银两), Entity 3=宝箱(溪山行旅图)）
> choose 1           ← 选择林平之（对话，触发剧情战斗）
> choose 2           ← 搜索宝箱（得银两700+智慧果）
> choose 3           ← 搜索宝箱（得溪山行旅图+松风剑谱+菩提子）
> leave              ← 返回大地图
```

### Step 2: 天宁寺/《连城诀》（存到槽位1）
```
> list
...找到"天寧寺"的索引...
> choose <idx>       ← 导航到天宁寺
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 1           ← 选择 NPC（oldevent_605）
> choose 1           ← 选择"对话"
（天宁寺对话提示）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 3: 梅庄/黑木令（读取槽位1，需有广陵散）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"梅莊"的索引...
> choose <idx>       ← 导航到梅庄
（显示"你来到了"）
> choose 1           ← 选择丹青生（对话四庄主连环挑战）
（需有梨花酒→秃笔翁→黑白子→黄钟公）
（过程中需使用广陵散琴曲触发地牢入口）
> choose <N>         ← 进入梅庄地牢
（到达梅庄地牢，寻得吸星大法+黑木令牌）
> leave              ← 返回大地图
```
（注：光明顶六大派围攻战斗已验证）

### Step 4: 黑木崖/守卫对话（取黑木令牌进场线索）
```
> list
...找到"黑木崖"的索引...
> choose <idx>       ← 导航到黑木崖
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 1           ← 选择黑木崖守卫（第1NPC，oldevent_316）
> choose 1           ← 选择"对话"
（守卫对话，提示需要黑木令牌）
> leave              ← 返回大地图
```
（注：需先在 P7 Step3 梅庄地牢获得黑木令牌才能进入黑木崖内部）

### Step 5: 黑木崖内部——东方不败战斗得《笑傲江湖》（读取槽位1，需有黑木令牌）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"黑木崖"的索引...
> choose <idx>       ← 导航到黑木崖
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 2           ← 选择黑木崖内部入口（使用黑木令牌进入）
（进入黑木崖内部大殿，显示东方不败+任我行+梅庄四友剧情）
> choose 1           ← 触发对话剧情
（多页对话连续 choose 1 跳过）
（触发战斗[54]或[57]，如任我行在队中则不同版本）
> look               ← 查看战场态势
（战斗循环：choose 5→1→1→1 直到"战斗胜利"）
（胜利后得《笑傲江湖》item 151）
> leave              ← 返回大地图
```

### Step 6: 丐帮/乔峰对话（第一次，接《天龙八部》线索，存到槽位2）
```
> list
...找到"丐幫"的索引...
> choose <idx>       ← 导航到丐帮
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 3           ← 选择乔峰（第3NPC，oldevent_525）
> choose 1           ← 选择"对话"
（乔峰对话剧情，多页对话连续 choose 1 跳过）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 2`（存到槽位2）

### Step 7: 桃花岛/《射雕英雄传》（读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"桃花島"的索引...
> choose <idx>       ← 导航到桃花岛
（显示"你来到了"）
> look               ← 查看场景 NPC
> choose 1           ← 选择黄蓉（第1NPC，oldevent_466）
> choose 1           ← 选择"对话"
（黄蓉对话剧情：品德90+则直接得《射雕英雄传》；品德不足则黄蓉设置战斗入口 oldevent_469）
> choose 0           ← 关闭对话框
> look               ← 若品德不足重新查看（出现 oldevent_469 战斗入口）
> choose <N>         ← 选择战斗入口（oldevent_469，挑战郭靖战斗[76]，胜利后也可得《射雕英雄传》）
> leave              ← 返回大地图
```
（注：品德90+直接得书；不足时通过战斗[76]挑战郭靖获得。品德可通过剧情选择提升（帮助 NPC、正义选项），起始 50）

### Step 8: 主角居/搜刮物品
```
> list
...找到"主角的家"的索引...
> choose <idx>       ← 导航到主角居
（显示"你来到了"，Entity 1~4=宝箱, Entity 5=软体娃娃）
> choose 1           ← 搜索（得精气丸×3）
> choose 2           ← 搜索（得人蔘×3）
> choose 3           ← 搜索（得小还丹×3）
> choose 4           ← 搜索（得银两）
> leave              ← 返回大地图
```

### Step 9: 丐帮二刷——乔峰战斗得《天龙八部》（读取槽位2，需燕子坞大燕图表线已完成）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"丐幫"的索引...
> choose <idx>       ← 导航到丐帮
（显示"你来到了"）
> look               ← 查看场景实体（D* 动态扫描显示 olodevent_527/528）
> choose <N>         ← 选择动态实体（oldevent_527 乔峰对话，问天龙八部下落）
（乔峰告知：需打败他才能得《天龙八部》，设置战斗入口 528）
> look               ← 重新查看（此时出现 oldevent_528 战斗入口）
> choose <N>         ← 选择动态实体（oldevent_528，挑战乔峰）
> choose 5           ← 选"战斗"（与乔峰切磋，进入战斗[83]）
> look               ← 查看战场态势
（战斗循环：choose 5→1→1→1 直到"战斗胜利"）
（胜利后乔峰赠送《天龙八部》item 147）
> leave              ← 返回大地图
```
（注：天龙八部前置链：P3 Step6 燕子坞使用大燕皇帝世系图表(oldevent_573)告知书在乔峰处 → 丐帮二刷触发 527→528。若燕子坞未使用大燕图表，527 不会出现。）

### Step 10: 天宁寺二刷——《连城诀》（需唐诗选辑，读取槽位2）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 12         ← 读取槽位2
> list
...找到"北丑居"的索引...
> choose <idx>       ← 先到北丑居（scene 6）
（显示"你来到了"）
> look               ← 查看场景实体
> choose <搜索>      ← 选择"搜索"实体触发 oldevent_603（使用唐诗选辑160，浮现"连城诀…天宁寺大殿佛像后"，并设置天宁寺 tile18=644）
> menu              ← 若搜索未触发，改用物品菜单：menu → 4(物品) → 1(使用) → 唐诗选辑(160) → 目标 oldevent_15（tile0 所在格）
> leave              ← 返回大地图
> list
...找到"天寧寺"的索引...
> choose <idx>       ← 导航到天宁寺
（显示"你来到了"）
> look               ← 查看场景实体
> choose 1           ← 查看碑文（oldevent_605，显示"上面写着：天宁寺"）
> choose <搜索>      ← 选择"搜索"实体触发 oldevent_644 掘墓（得《连城诀》item 146）
> leave              ← 返回大地图
```
（注：连城诀完整获取链：唐诗山洞(scene 65, 364:279) 搜索 oldevent_602 得唐诗选辑(160) → 北丑居 look 选择"搜索"(oldevent_603，tile0 静态 eventTouch=603) 使用唐诗选辑（浮现"连城诀…天宁寺大殿佛像后"，603 通过 instruct_4(160) 检查背包并设置天宁寺 tile18=644）→ 回天宁寺 look 选择"搜索"(oldevent_644 掘墓) 得《连城诀》item 146。603/644 均为 D* 动态事件，由 look() 运行时扫描显示。）

---

## P8: 闯王山洞→鸳鸯岛

### Step 1: 闯王山洞（雪山区 scene 5，列表第19项，读取槽位1 → 存到槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
（场景列表中有多个同名"山洞"，用坐标区分：闯王山洞=雪山坐标[64:51]，
  鸳鸯岛=[425:38]，唐诗山洞=[364:279]。列表第19项"山洞"是雪山区的闯王山洞）
> choose 19          ← 导航到闯王山洞
（显示"你来到了"）
> look               ← 查看场景实体（动态"搜索"列表，实测约 2=雪山飞狐145 / 3=鸯刀188 / 4=金丝背心121）
> choose <N>         ← 依次选择搜索实体，直到得齐：《雪山飞狐》+ 鸯刀 + 金丝背心
（实测实体索引：oldevent_54 雪山飞狐 / oldevent_56 鸯刀 / oldevent_58 金丝背心，
  实体列表为动态构建，固定索引不可靠——look 后逐个 choose，拿到后 leave）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 2: 鸳鸯岛（东北角 scene 79，列表第18项，读取槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
（在场景列表中找到坐标为[425:38]的"山洞"，即东北角的鸳鸯岛 scene 79）
> choose <idx>       ← 按坐标找到鸳鸯岛索引后导航
（显示"你来到了"）
> look               ← 查看场景实体（动态"搜索"列表）
> choose <N>         ← 依次选择搜索实体，直到得《鸳鸯刀》（oldevent_651，item 157）
（实测鸳鸯刀在实体5附近；实体列表为动态构建，固定索引不可靠——look 后逐个 choose）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 3: 苗人凤居——《飞狐外传》（需胡斐+屠龙刀+金丝背心，读取槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"苗人鳳居"的索引...
> choose <idx>       ← 导航到苗人凤居
（显示"你来到了"）
> look               ← 查看场景实体
> choose <N>         ← 选择苗人凤（触发 oldevent_7）
（胡斐在队伍中，屠龙刀+金丝背心触发剧情）
（苗人凤收下屠龙刀和金丝背心, 得《飞狐外传》天书）
> leave              ← 返回大地图
```
（注：此步需先完成：胡斐加入(P2)、屠龙刀(P6冰火岛)、金丝背心(P8闯王山洞)）

---

## P9: 霹雳堂→圣堂

### Step 1: 霹雳堂/孔八拉（scene 76，读取槽位1 → 存到槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"霹靂堂"的索引...
> choose <idx>       ← 导航到霹雳堂
（显示"你来到了"）
> look               ← 查看场景实体
> choose 1           ← 选择 NPC 孔八拉（entity 1）
> choose 1           ← 选择"对话"
（第一次对话触发 oldevent_678，得霹雳弹×10、霹雳秘笈、黄钥匙）
> choose 1           ← 再次选择 NPC 孔八拉
> choose 1           ← 选择"对话"
（第二次对话触发动态事件解析，oldevent_679 或 686）
（如果已获得绿钥匙，显示绿钥匙相关文本）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

**圣堂入口说明**: 圣堂（scene 83）无地图入口，需通过霹雳堂出口进入。当前已验证完整流程：
- ✅ 华山论剑 → 获得神杖（P10 已验证）
- ✅ 霹雳堂孔八拉 → 出示神杖 → 获得绿钥匙（P10 已验证）
- ✅ 从霹雳堂出口进入圣堂（P10 已验证，look 查看出口列表 → choose 出口编号）
- ✅ 圣堂内14个书架（放置天书事件）可见并可操作（P10 已验证，无天书时事件正常返回）
- 放置全部14天书 → 触发十大高手战斗 → 通关动画（oldevent_1015→1016→1017，需收集全部14本天书才能触发）

---

## P10: 武道大会→霹雳堂→圣堂通关

### Step 1: 武道大会/华山论剑（scene 25，读取槽位1 → 存到槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"武道大會"的索引...
> choose <idx>       ← 导航到武道大会
（显示"你来到了"）
> choose 2           ← 选择 Entity 2=华山论剑（tile 事件，extra=936）
（触发华山论剑剧情，对话显示"华山论剑"/"神杖"/"岳不群"等）
> leave              ← 返回大地图
```
**存档**: `menu` → `choose 6` → `choose 1`（存到槽位1）

### Step 2: 霹雳堂 → 孔八拉 → 出示神杖 → 绿钥匙（读取槽位1）
```
> menu              ← 打开主选单
> choose 6          ← 选"系统（存档/读档）"
> choose 11         ← 读取槽位1
> list
...找到"霹靂堂"的索引...
> choose <idx>       ← 导航到霹雳堂
> choose 1           ← 选择 NPC 孔八拉
> choose 1           ← 选择"对话"
（第一次对话触发 oldevent_678，instruct_3 修改 D* 表）
> choose 1           ← 再次选择 NPC 孔八拉
> choose 1           ← 选择"对话"
（第二次对话触发动态事件解析 oldevent_686 → 神杖检查）
（如果有神杖：显示"是否使用物品？"对话框，choose 1 确认）
（获得绿钥匙，显示"綠鑰匙"或"绿钥匙"）
> leave              ← 返回大地图
```

### Step 3: 圣堂入口（通过霹雳堂出口进入）
```
> look               ← 查看出口列表（NPC 之后有出口 entity）
> choose 6           ← 或 choose 7 选择圣堂出口
（显示"你来到了圣堂"）
> look               ← 查看圣堂场景，显示书架（14个"放置天书"）
> choose 1           ← 选择第一个书架尝试放置天书
（无天书时显示"是否使用物品"对话框，选否取消）
> leave              ← 返回大地图
```
（注：圣堂放置14天书 → 十大高手战斗 → 通关动画，需收集全部14本天书才能触发，oldevent_1015→1016→1017）

---

## 14天书对应表

| # | 天书 | 获取位置 | MUD场景ID | 实际获取事件 | 前置条件 | Walkthrough 状态 |
|---|------|----------|-----------|-------------|---------|-----------------|
| 1 | 飞狐外传 | 苗人凤居 | scene 24 | oldevent_7 | 胡斐+屠龙刀+金丝背心 | ✅ P8 Step3 |
| 2 | **雪山飞狐** | **闯王山洞** | **scene 5** | oldevent_54 | 无 | ✅ P8 Step1 |
| 3 | 连城诀 | 天宁寺 | scene 63 | oldevent_644（掘墓） | 唐诗选辑（唐诗山洞602）→603→644 | ✅ P7 Step10（需唐诗选辑链） |
| 4 | 天龙八部 | 丐帮 | scene 51 | oldevent_528（战斗[83]） | 燕子坞大燕图表573→527→528 | ✅ P7 Step9（需二刷） |
| 5 | 射雕英雄传 | 桃花岛 | scene 75 | oldevent_466 | 品德90+ 或战斗[76]（oldevent_469） | ✅ P7 Step7（品德或战斗） |
| 6 | 白马啸西风 | 沙漠废墟→高昌迷宫 | scene 14 | oldevent_656（迷宫内） | 沙漠废墟出口进迷宫 | ✅ P6 Step5（choose 3） |
| 7 | 鹿鼎记 | 神龙教 | scene 71 | oldevent_611（战斗[95]） | 五毒教616线→611 | ✅ P6 Step13（需三刷） |
| 8 | 笑傲江湖 | 黑木崖 | scene 26 | oldevent_320（战斗[54/57]） | 黑木令牌+梅庄地牢 | ✅ P7 Step5（需令牌） |
| 9 | 书剑恩仇录 | 回族部落 | scene 17 | oldevent_622（交还可兰经） | 金轮寺得可兰经 | ✅ P6 Step11（二刷交书） |
| 10 | 神雕侠侣 | 古墓 | scene 18 | oldevent_443 | 无（绝情谷底后） | ✅ P3 Step5 |
| 11 | 侠客行 | 侠客岛 | scene 74 | oldevent_363（石破天解太玄经） | 摩天崖石破天加入 | ✅ P6 Step9b（需二刷） |
| 12 | 倚天屠龙记 | 光明顶 | scene 11 | oldevent_115（圣火阵战斗[15]） | 冰火岛谢逊线+铁焰令 | ✅ P6 Step14（需二刷） |
| 13 | 碧血剑 | 渤泥岛 | scene 78 | oldevent_636（道德检查） | 金蛇剑+道德80+ | ✅ P6 Step9c（需二刷） |
| 14 | **鸳鸯刀** | **鸳鸯岛** | **scene 79** | oldevent_651 | 无物品检查（原版需鸳刀+鸯刀开墙孔，web MUD 直接可得） | ✅ P8 Step2 |

### 闯王山洞 (scene 5) 关键事件
- oldevent_54 → 得 **《雪山飞狐》**(item 145), 获天书
- oldevent_55 → 得 **满天花雨**(item 88)
- oldevent_56 → 得 **鸯刀**(item 188)
- oldevent_57 → 得 **银两3000**
- oldevent_58 → 得 **金丝背心**(item 121)

### 鸳鸯岛 (scene 79) 关键事件
- oldevent_645 → 墙上长刀孔(需鸳刀)
- oldevent_646 → 墙上短刀孔(需鸯刀)
- oldevent_651 → 得 **《鸳鸯刀》**(item 157), 获天书
- oldevent_652 → 得 **千年人参**(item 34)

### 圣堂通关流程
- **华山论剑** (武道大会 scene 25) → oldevent_936 → 比武获胜 → 得**神杖**(item 143)
- **霹雳堂** (scene 76) → 孔八拉对话 → 出示神杖 → 得**绿钥匙**(item 164)
- **圣堂** (scene 83) → 放置14天书 → 十大高手战斗 → 通关动画

---

## 场景 look 列表重复项说明

**检查方法**: 复刻 `SmapHandlers.look` 实体列表生成逻辑（`scripts/check_scene_dupes.js`），
静态计算全部 84 个小场景**首次进入**时的 look 内容列表，检测重复项。结论：40/84 场景存在重复项，
分两类：

### 1. 数据本身如此（正常，不是问题）

- **同名场景**: 4 个"山洞"（scene 5/41/65/79），攻略已按坐标区分（唐诗山东=scene 65）
- **多出口同目标**: 明教分舵/沙漠废墟/梅庄/崑崙仙境/霹雳堂 各有 3 个出口指向同一场景，
  原版数据即为多出口
- **同事件多 tile**: 如大轮寺钥匙孔(oldevent_598)在 tile 6/7 都有 eventTouch → 显示 2 个"搜索"
- **物品列表**: 0 个场景存在物品重复

### 2. 显示层重复（look() 双源扫描冗余，功能无碍）

D* 表（`JY.D[sceneId]` 由 events.json 初始化）扫描与 events.json grid 扫描**同时**列出同一事件，
导致同一"搜索"出现 2 次（如大轮寺 596、苗人凤居 30/7、蜘蛛洞 375~380、圣堂天书事件等）。
圣堂天书事件尤为明显：每个天书位同时显示"搜索"（D* 扫描）与"放置天书"（grid 扫描）。
**choose 任一都会触发同一事件**，消费后 `eventConsumed` 使两个来源同时消失，不影响任何流程
（walkthrough p1~p10 全绿即为证据）。

---

## 地图坐标
```
1-主角居[357:235] 2-河洛客棧[359:229] 3-梅莊[330:196] 4-燕子塢[308:197]
5-福威鏢局[369:258] 6-山洞(唐詩選輯)[364:279] 7-田伯光居[385:306] 8-南賢居[388:325]
9-閻基居[396:374] 10-天寧寺[330:237] 11-藥王莊[323:318] 12-衡山派[355:376]
13-一燈居[314:370] 14-黑龍潭[309:351] 15-鐵掌山[302:343] 16-程瑛居[295:422]
17-五毒教[247:424] 18-峨嵋派[215:408] 19-高昇客棧[199:401] 20-無量山洞[169:426]
21-山洞2[172:425] 22-凌霄城[166:404] 23-白駝山[141:433] 24-金輪寺[103:397]
25-星宿海[132:410] 26-龍門客棧[102:374] 27-回族部落[120:343] 28-明教分舵[76:363]
29-光明頂[68:397] 30-崑崙派[42:420] 31-崑崙仙境[22:440] 32-冰蠶洞[22:449]
33-摩天崖[186:338] 34-青城派[185:370] 35-武當派[226:340] 36-崆峒派[160:314]
37-思過崖[187:301] 38-華山派[193:302] 39-武道大會[192:295] 40-金蛇山洞[193:293]
41-絕情谷[134:259] 42-絕情谷底[132:254] 43-百花谷[159:271] 44-悅來客棧[221:286]
45-丐幫[229:256] 46-少林寺[247:300] 47-嵩山派[242:293] 48-蝴蝶谷[238:220]
49-泰山派[193:139] 50-平一指居[215:196] 51-苗人鳳居[223:187] 52-海邊小屋[286:157]
53-大輪寺[114:297] 54-薛慕華居[192:261] 55-擂鼓山[176:221] 56-洪七公居[168:211]
57-古墓[144:218] 58-重陽宮[145:217] 59-恒山派[112:215] 60-黑木崖[118:178]
61-成崑居[19:249] 62-蜘蛛洞[75:226] 63-沙漠廢墟[53:219] 64-神雕洞[68:170]
65-破廟[19:181] 66-有間客棧[78:141] 67-北丑居[51:109] 68-胡斐居[86:94]
69-雲鶴崖[17:15] 70-闯王山洞(雪山)[64:51] 71-小船[108:100] 72-冰火島[103:16]
73-神龍島[141:113] 74-靈蛇島[187:63] 75-浡泥島[277:43] 76-桃花島[341:138]
77-霹靂堂(聖堂入口)[409:217] 78-海外山洞(鸳鸯岛)[425:38] 79-萬鱷島[432:125] 80-俠客島[455:328]
```

注：MUD 数据中场景编号与地图坐标已匹配。场景5（闯王山洞）和场景79（鸳鸯岛）在数据中标注为"山洞"，但通过坐标可确认身份。

---

# 原版攻略参考

## 原版快速通关
```
主角居→南贤→田伯光居/鸯刀→唐诗山东→闫基/胡家刀法/天王保命丹
→铁掌帮/大燕族谱→高升客栈/段誉加入→无量山洞（主角练凌波微步）
→回族部落→胡斐加入→苗人凤居/退敌→冰火岛/金毛→绝情谷/玉玺/断肠草/君子剑/玉蜂针/药材100
→冰蚕洞/天山雪莲→大轮寺→昆仑仙境/张无忌加入/九阳真经→神雕洞/救杨过
→百花谷/养蜂→绝情谷底→古墓/小龙女加入/神雕侠侣→燕子坞/慕容复、王语嫣加入
→开船→神龙教/鹿鼎记→泰山派/洗手帖→蝴蝶谷/胡青牛加入
→悦来客栈/令狐冲喝酒→摩天崖→五毒教→程瑛加入→黑龙潭→一灯居→黑龙潭(二刷)
→闫基居/七星海棠→药王庄/程灵素加入→衡山派→金轮寺/可兰经
→明教分舵/乾坤大挪移→光明顶/六大派→华山派→金蛇洞/金蛇剑→武当山
→嵩山/张旭率意帖→神龙教→闯王山洞/雪山飞狐→破庙→成昆居→沙漠废墟/白马啸西风
→北丑居→冰火岛/屠龙刀→光明顶/找龙王→回族部落/书剑恩仇录→灵蛇岛/紫衫龙王
→渤泥岛/碧血剑→鸳鸯岛/鸳鸯刀→侠客岛/侠客行→福威镖局→天宁寺/连城诀
→梅庄/黑木令→光明顶→黑木崖/笑傲江湖→丐帮/天龙八部→苗人凤居/飞狐外传
→桃花岛/射雕英雄传→主角居→华山论剑→圣堂
```